overwrite: true
schema: "https://rickandmortyapi.com/graphql/"
documents: "apollo/queries/**.{ts.tsx}"
generates:
  apollo/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      withHooks: true